# Pre-commit configuration for SlopGuard
repos:
  - repo: local
    hooks:
      - id: slopguard-presubmit
        name: SlopGuard Leak & Presubmit Gatekeeper
        entry: python3 scripts/presubmit.py --staged
        language: system
        stages: [commit]
