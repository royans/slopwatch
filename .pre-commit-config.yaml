# Pre-commit configuration for SlopWatch
repos:
  - repo: local
    hooks:
      - id: slopwatch-presubmit
        name: SlopWatch Leak & Presubmit Gatekeeper
        entry: python3 scripts/presubmit.py --staged
        language: system
        stages: [commit]
