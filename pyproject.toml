[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sentinel"
version = "0.1.0"
description = "High-throughput deterministic supply chain malware, typosquatting, and AI hallucination detection engine."
readme = "README.md"
authors = [
    { name = "Royans K" },
]
license = { text = "Apache-2.0" }
requires-python = ">=3.11"
keywords = [
    "security",
    "supply-chain",
    "malware-analysis",
    "ast-analysis",
    "yara",
    "typosquatting",
    "phantom-squatting",
    "ai-hallucination",
    "dependency-auditing",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: Information Technology",
    "Intended Audience :: System Administrators",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Security",
    "Topic :: Software Development :: Build Tools",
    "Topic :: Software Development :: Quality Assurance",
]
dependencies = [
    "aiohttp>=3.9.0",
    "aiodns>=3.1.0",
    "aiosqlite>=0.19.0",
    "sqlalchemy[asyncio]>=2.0.0",
    "pydantic>=2.5.0",
    "pyyaml>=6.0",
    "rich>=13.7.0",
    "click>=8.1.0",
    "dnspython>=2.5.0",
    "yara-python>=4.5.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-mock>=3.12.0",
    "aioresponses>=0.7.6",
    "build>=1.0.0",
    "twine>=5.0.0",
]

[project.urls]
Homepage = "https://github.com/royans/sentinel/"
Documentation = "https://github.com/royans/sentinel/#readme"
Repository = "https://github.com/royans/sentinel/"
Issues = "https://github.com/royans/sentinel/issues"

[project.scripts]
sentinel = "sentinel.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"sentinel" = [
    "rules/*.yar",
    "signatures/*.json",
]

[tool.pytest.ini_options]
pythonpath = ["src", "."]
asyncio_mode = "auto"
